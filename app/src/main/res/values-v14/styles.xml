<resources>

    <!--
        Base application theme for API 14+. This theme completely replaces
        AppBaseTheme from BOTH res/values/styles.xml and
        res/values-v11/styles.xml on API 14+ devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- API 14 theme customizations can go here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="PopmenuStyle" parent="Widget.AppCompat.Light.PopupMenu" >
        <item name="android:layout_width" >300dp</item>
        <item name="android:dropDownWidth" >400dp</item>
        <item name="android:dropDownVerticalOffset" >40dp</item>
        <item name="android:dropDownHorizontalOffset" >40dp</item>
        <item name="android:popupBackground" >@color/theme_red_accent</item>

        <item name="android:background" >@color/app_color_normal</item>
        </style>

    <style name="TranslucentStatus" parent="AppBaseTheme" />
</resources>
