apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

task hello {
    doLast{
        println "Hello Word!"
    }
}

println "${project.name} buildWithTinker=" + buildWithTinker();
