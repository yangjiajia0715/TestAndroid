<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testandroid"
    android:versionCode="1"
    android:versionName="1.0">
    <!--18-->
    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="24" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--name MultiDexAppcation-->
    <application
        android:name=".tinker.SampleApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.ZoomImageActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen"><!-- ATTENTION: This intent was auto-generated. Follow instructions at
  https://g.co/AppIndexing/AndroidStudio to publish your URLs. -->
        </activity>

        <activity
            android:name=".activity.ViewPagerActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen" />

        <activity
            android:name=".activity.ParallaxActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen" />

        <activity
            android:name=".activity.ListViewClipActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <activity
            android:name=".activity.ScrollViewClipActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <activity
            android:name=".activity.ViewPagerAllActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <activity
            android:name=".ObservableScrollView.demo.ParallaxToolbarScrollViewActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <activity android:name=".ObservableScrollView.viewpager.ViewPagerTabActivity" />

        <activity
            android:name=".activity.AnimationActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <activity
            android:name=".activity.FrgStatePagerAdaperActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <activity
            android:name=".activity.ItemTypeListViewActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <activity
            android:name="com.example.testandroid.activity.DexClassLoaderActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <activity
            android:name="com.example.testandroid.activity.ViewAnimatorActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <activity
            android:name="com.example.testandroid.activity.CheckTextViewActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <activity
            android:name="com.example.testandroid.activity.DexClassLoadTestActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <activity
            android:name="com.example.testandroid.activity.PhotoViewActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <activity
            android:name="com.example.testandroid.activity.BuildConfigFieldActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <activity
            android:name="com.example.testandroid.activity.TestTinkerActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <activity
            android:name="com.example.testandroid.activity.RxJavaActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <activity
            android:name="com.example.testandroid.activity.CursorLoadActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />

        <service
            android:name=".service.SampleResultService"
            android:exported="false" />

        <activity
            android:name="com.example.testandroid.activity.DataBindingActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar" />
    </application>

</manifest>